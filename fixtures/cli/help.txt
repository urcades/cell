pi-rust - AI coding assistant with read, bash, edit, write tools

Usage:
  pi-rust [options] [@files...] [messages...]

Commands:
  pi-rust install <source> [-l]    Install extension source and add to settings
  pi-rust remove <source> [-l]     Remove extension source from settings
  pi-rust update [source]          Update installed extensions (skips pinned sources)
  pi-rust list                     List installed extensions from settings
  pi-rust config                   Open TUI to enable/disable package resources
  pi-rust <command> --help         Show help for install/remove/update/list

Options:
  --provider <name>              Provider name (optional; inferred from model/auth)
  --model <pattern>              Model pattern or ID (supports "provider/id" and optional ":<thinking>")
  --api-key <key>                API key (defaults to env vars)
  --system-prompt <text>         System prompt (default: coding assistant prompt)
  --append-system-prompt <text>  Append text or file contents to the system prompt
  --mode <mode>                  Output mode: text (default), json, or rpc
  --print, -p                    Non-interactive mode: process prompt and exit
  --continue, -c                 Continue previous session
  --resume, -r                   Select a session to resume
  --session <path>               Use specific session file
  --session-dir <dir>            Directory for session storage and lookup
  --no-session                   Don't save session (ephemeral)
  --models <patterns>            Comma-separated model patterns for Ctrl+P cycling
                                 Supports globs (anthropic/*, *sonnet*) and fuzzy matching
  --no-tools                     Disable all built-in tools
  --tools <tools>                Comma-separated list of tools to enable (default: read,bash,edit,write)
                                 Available: read, bash, edit, write, grep, find, ls
  --thinking <level>             Set thinking level: off, minimal, low, medium, high, xhigh
  --extension, -e <path>         Load an extension file (can be used multiple times)
  --no-extensions, -ne           Disable extension discovery (explicit -e paths still work)
  --skill <path>                 Load a skill file or directory (can be used multiple times)
  --no-skills, -ns               Disable skills discovery and loading
  --prompt-template <path>       Load a prompt template file or directory (can be used multiple times)
  --no-prompt-templates, -np     Disable prompt template discovery and loading
  --theme <path>                 Load a theme file or directory (can be used multiple times)
  --no-themes                    Disable theme discovery and loading
  --export <file>                Export session file to HTML and exit
  --list-models [search]         List available models (with optional fuzzy search)
  --verbose                      Force verbose startup (overrides quietStartup setting)
  --help, -h                     Show this help
  --version, -v                  Show version number

Extensions can register additional flags (e.g., --plan from plan-mode extension).

Examples:
  # Interactive mode
  pi-rust

  # Interactive mode with initial prompt
  pi-rust "List all .ts files in src/"

  # Include files in initial message
  pi-rust @prompt.md @image.png "What color is the sky?"

  # Non-interactive mode (process and exit)
  pi-rust -p "List all .ts files in src/"

  # Multiple messages (interactive)
  pi-rust "Read package.json" "What dependencies do we have?"

  # Continue previous session
  pi-rust --continue "What did we discuss?"

  # Use different model
  pi-rust --provider openai --model gpt-4o-mini "Help me refactor this code"

  # Use model with provider prefix (no --provider needed)
  pi-rust --model openai/gpt-4o "Help me refactor this code"

  # Use model with thinking level shorthand
  pi-rust --model sonnet:high "Solve this complex problem"

  # Limit model cycling to specific models
  pi-rust --models claude-sonnet,claude-haiku,gpt-4o

  # Limit to a specific provider with glob pattern
  pi-rust --models "openrouter/*"

  # Cycle models with fixed thinking levels
  pi-rust --models sonnet:high,haiku:low

  # Start with a specific thinking level
  pi-rust --thinking high "Solve this complex problem"

  # Read-only mode (no file modifications possible)
  pi-rust --tools read,grep,find,ls -p "Review the code in src/"

  # Export a session file to HTML
  pi-rust --export ~/.pi-rust/agent/sessions/--path--/session.jsonl
  pi-rust --export session.jsonl output.html

Environment Variables:
  ANTHROPIC_API_KEY                - Anthropic Claude API key
  ANTHROPIC_OAUTH_TOKEN            - Anthropic OAuth token (alternative to API key)
  OPENAI_API_KEY                   - OpenAI GPT API key
  OPENROUTER_API_KEY               - OpenRouter API key
  PI_RUST_CODING_AGENT_DIR         - Session storage directory (default: ~/.pi-rust/agent)
  PI_PACKAGE_DIR                   - Override package directory (for Nix/Guix store paths)
  PI_SHARE_VIEWER_URL              - Base URL for /share command (default: https://pi.dev/session/)

Available Tools (default: read, bash, edit, write):
  read   - Read file contents
  bash   - Execute bash commands
  edit   - Edit files with find/replace
  write  - Write files (creates/overwrites)
  grep   - Search file contents (read-only, off by default)
  find   - Find files by glob pattern (read-only, off by default)
  ls     - List directory contents (read-only, off by default)
